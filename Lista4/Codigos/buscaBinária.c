#include <stdio.h>
#include <stdio.h>

int buscaBinaria(){

}

int main(){
    int ordenados, procurados;
    scanf("%d %d", &ordenados, &procurados);
    int vetor[ordenados], numeros;
    for(int i = 0; i < ordenados; i++){
        scanf("%d", &vetor[i]);
    }
    for(int j = 0; j < procurados; j++){
        scanf("%d", &numeros);

 

 
    }
    return 0;
}